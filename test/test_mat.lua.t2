:: test_mat.lua
; reload
; start debugger
; stop debugger
; playground

;; start debugger
require"osv".launch { port = 8086 }

;; stop debugger
require"osv".stop()

;; reload
package.loaded["swan"] = nil
swan = require"swan"
p = function(s) io.write(tostring(s)) end
c = swan.constant

;; playground
; reload
local a,b,c,d = swan.symbols "a_1 b c d"
local m = swan.mat {{a,b}, {c,d}}
p(m)
