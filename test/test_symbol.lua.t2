:: test_symbol.lua
; reload
; start debugger
; stop debugger
; playground

;; reload
package.loaded["swan"] = nil
swan = require"swan"
p = function(s) io.write(tostring(s) .. "\n") end
c = swan.constant

;; start debugger
require"osv".launch { port = 8086 }

;; stop debugger
require"osv".stop()

;; playground
; reload
local r1 = swan.syms("A", swan.matrix_set:new(2,2))
local a = swan.syms("a")
r1:assign({{a,a},{a,a}})
p(r1)

;; playground
; reload
local g = swan.grid(
